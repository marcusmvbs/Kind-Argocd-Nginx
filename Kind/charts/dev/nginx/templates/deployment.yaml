apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.nginx.deployment.name }}
  namespace: {{ .Values.nginx.deployment.namespace }}
  labels:
    app: nginx
spec:
  replicas: {{ .Values.nginx.deployment.replicas }}
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: {{ .Values.nginx.deployment.image }}
        resources: 
          limits:
            cpu: {{ .Values.nginx.deployment.cpu }}
            memory: {{ .Values.nginx.deployment.memory }}
        ports:
        - containerPort1: {{ .Values.nginx.deployment.ports.containerPort1 }}
        - containerPort2: {{ .Values.nginx.deployment.ports.containerPort2 }}
      dnsPolicy: {{ .Values.nginx.deployment.dnsPolicy }}
      restartPolicy: {{ .Values.nginx.deployment.restartPolicy }}